/**
 * Drizzle schema barrel. Tables are organized by data layer
 * (raw / staging / mart / model / ops) per CLAUDE.md 데이터 레이어 규약.
 *
 * Full table definitions land in solar-8wv.8 (DB schema 초안, §9).
 * Kept empty-but-present so drizzle-kit and downstream imports resolve.
 */
export {};
